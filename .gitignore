node_modules/
dist/

# Yarn keeps its cache and unplugged deps here; the lockfile is committed.
.yarn/*
!.yarn/patches
!.yarn/releases
.pnp.*

# Worktree for the deploy branch, created by scripts/deploy.sh.
.deploy/
.env
.env.local
*.log
.DS_Store
tools/verify/shots/

# Legally-dumped ROM sets. NEVER commit these.
roms/*
!roms/README.md

# Emscripten toolchain, FBNeo checkout and core build output.
tools/core/.work/
tools/core/out/

# Local notes, kept out of the repository.
/ARCHITECTURE.md
/CLAUDE.md

# ROMs

Put your own legally-dumped MAME `dino` ROM set here as `roms/dino.zip`.

This directory is gitignored (everything except this file). Nothing in here is
ever committed, and this project will never download a ROM for you.
